apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: source-api
  labels:
    app.kubernetes.io/name: source-api
spec:
  hosts:
    - source-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: source-api
            port:
              number: 80
          weight: 100
